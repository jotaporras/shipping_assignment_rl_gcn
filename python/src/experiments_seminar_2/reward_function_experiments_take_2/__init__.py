"""
  # Reward function experiments take 2
  After training stability improvements and trying as much as possible to not depend
  on learning to avoid Big Ms, this is a quick test of the reward functions that exist.
  It's separated by env size (different from the previous set of reward experiments).
"""
